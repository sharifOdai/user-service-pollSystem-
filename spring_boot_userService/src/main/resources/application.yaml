server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: userService
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

pollService:
  url: http://localhost:8082/user-answer